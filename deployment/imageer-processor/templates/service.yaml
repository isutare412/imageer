apiVersion: v1
kind: Service
metadata:
  name: {{ include "imageer-processor.fullname" . }}
  labels:
    {{- include "imageer-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imageer-processor.selectorLabels" . | nindent 4 }}
