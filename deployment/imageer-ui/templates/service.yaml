apiVersion: v1
kind: Service
metadata:
  name: {{ include "imageer-ui.fullname" . }}
  labels:
    {{- include "imageer-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imageer-ui.selectorLabels" . | nindent 4 }}
