apiVersion: v1
kind: Service
metadata:
  name: {{ include "imageer-gateway.fullname" . }}
  labels:
    {{- include "imageer-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "imageer-gateway.selectorLabels" . | nindent 4 }}
